/*
 * @name changeImage(image,compressionQuality)
 * @description this method returns the specified image in JPEG format. The quality of the resulting JPEG image,expressed as a value from 0.0 to 1.0. the value 0.0 represents the maximum compression(or lowest quality) while the value 1.0 represents the least compression (or best quality).
 * @author AutoWork
 *
 */
var image=getClipImage();
saveToAlbum(changeImage(getClipImage(),0.8);
